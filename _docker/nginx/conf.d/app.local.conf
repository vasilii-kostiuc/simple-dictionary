server {

    root /var/www/simple_dictionary_api/public;

    access_log /var/log/nginx/access.log;
    error_log /var/log/nginx/error.log;

    location / {
        try_files $uri /index.php?$query_string;
    }

    location ~ \.php$ {
        try_files $uri =404;
        fastcgi_split_path_info ^(.+\.php)(/.+)$;
        fastcgi_pass app:9000;
        fastcgi_index index.php;
        include fastcgi_params;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        fastcgi_param PATH_INFO $fastcgi_path_info;
    }

    # WSS
    # resolver — forces nginx to re-resolve wss_app on each request (valid=5s),
    # preventing stale IP caching when switching between single-node and multinode modes.
    resolver 127.0.0.11 valid=5s;
    location /ws/ {
        set $wss_upstream http://wss_app:8080;
        proxy_pass $wss_upstream;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_read_timeout 86400;
        proxy_send_timeout 3600;
    }
}



